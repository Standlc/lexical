{
    "name": "@lexical.app/server",
    "module": "src/index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "start": "bun run src/index.ts",
        "dev": "bun run --hot src/index.ts",
        "build": "echo 'No build needed for backend'",
        "lint": "eslint .",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/bun": "^1.3.0"
    },
    "dependencies": {
        "pg": "^8.16.3"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
